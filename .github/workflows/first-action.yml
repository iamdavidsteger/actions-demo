name: First Workflow
on: workflow_dispatch
jobs:
    first-job:
      runs-on: ubuntu-latest
      steps:
        - name: Print greeting
          run: |
              echo "Hi Dave!"
              echo "Wie gehts?"
        - name: Print goodbye
          run: echo "Bye Dave!"
